{
  "name": "reta-ui-workspace",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter gallery dev",
    "build": "pnpm --filter gallery build",
    "preview": "pnpm --filter gallery preview",
    "test": "pnpm --filter reta-ui test && pnpm --filter gallery test",
    "lint": "turbo run lint",
    "fix": "turbo run fix",
    "gallery:dev": "pnpm --filter gallery dev",
    "gallery:build": "pnpm --filter gallery build",
    "gallery:test": "pnpm --filter gallery test",
    "ui:test": "pnpm --filter reta-ui test"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.10",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
